spring:
  profiles:
    include: apidef
logging:
  pattern:
    console: "%d{dd-MM-yyyy HH:mm:ss.SSS}|%level|%logger.%M|%X{sid}|%X{uuid}|%X{uri}|%X{method}|%msg%n"
  level:
    root: INFO
    com.github.ag.producer: debug
    com.github.ag.core: DEBUG
    